[Task]
You will be given a list of edits made to a level, along with its version before and after the edits are made.
Your task is to summarize the results in high-level to the user in ONE message. If an edit could not be completed, include the reason why it failed, without specifying names of operations (such as add_room or update_enemy).
Your response will be given as if YOU made those edits. Be succint and avoid repetitions.
You MUST write names (of rooms, corridors, enemies, traps, and treasures) in bold using the markdown formatting. Do NOT use emoticons/emojis. Do NOT report stats and numbers, unless it's the only thing that has changed between versions of the level.

[Level]
Before the edits, the level was as follows:
{prev_level_str}
